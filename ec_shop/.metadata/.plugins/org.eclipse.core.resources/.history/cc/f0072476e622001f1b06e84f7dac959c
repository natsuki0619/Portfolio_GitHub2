<!DOCTYPE html>
<html xmlns:th="https://www.thymeleaf.org" xmlns:sec="http://www.thymeleaf.org/extras/spring-security">
    <head>
         <div th:replace="~{fragment :: meta}"></div>
         <div th:replace="~{fragment :: styles}"></div>
        <title>EC SHOP</title>
    </head>
    <body>
         <div class="ecshop-wrapper">
             <!-- ヘッダー -->
             <div th:replace="~{fragment :: header}"></div>
             <main>
                <div class="container pt-4 pb-5 ecshop-container">
                    <div th:if="${param.loggedIn}" class="alert alert-secondary">
                        ログインしました。
                    </div>

                    <div th:if="${param.loggedOut}" class="alert alert-secondary">
                        ログアウトしました。
                    </div>

                     <div th:if="${successMessage}" class="alert alert-secondary">
                         <span th:text="${successMessage}"></span>
                     </div>
                     
                     <div class="shelf-container">
        <div class="product">
            <img src="images/goods01.png" alt="商品1">
        </div>
        <div class="product">
            <img src="商品画像2.jpg" alt="商品2">
        </div>
        <div class="product">
            <img src="商品画像3.jpg" alt="商品3">
        </div>
        <!-- 必要に応じて商品を追加 -->
    </div>

                </div>
            </main>
             <!-- フッター -->
             <div th:replace="~{fragment :: footer}"></div>
         </div>
             <div th:replace="~{fragment :: scripts}"></div>
    </body>
</html>